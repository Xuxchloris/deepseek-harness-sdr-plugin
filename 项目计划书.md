# ai-sdr — 外贸获客 AI 员工（项目计划书）

> 定位：把"客户开发 SOP"升级为能自主干活的 AI 员工。
> 一句话：派一条任务"开发 50 个美国户外用品客户"，AI 员工自动跑完
> **找客户 → 背调 → 评分 → 个性化开发信草稿 → 人工审批 → 跟进计划 → 报价素材**，
> 全程轨迹可回放，在飞书里接单与审批。

## 1. 业务背景（为什么做）

- 外贸 B2B 客户开发全流程依赖人工：找客户、公司背调、写开发信、跟进、报价，
  每个环节靠个人经验，新人无法复制，团队效率低（实习驻场真实观察）。
- 第一版（hermes-export-skills）用 Hermes Skills 沉淀了 SOP，发现问题：
  **文档是文档，Agent 不会真干活**——流程碎片化、无法自主执行、效果不可控。
- 本项目重写：Pydantic AI Agent 循环驱动全流程（LLM 主导 loop + MCP 插件 + 门控审批），
  人工只在关键节点审批。代码不继承第一版，业务理解继承。

## 2. 核心流程（SDR 工作流 → Agent 循环 + 阶段机）

```
任务输入（飞书/API）：「开发 50 个美国户外用品客户」
    │
    ▼
[1 任务解析]  拆解目标市场/产品/数量，产出执行计划
    ▼
[2 客户发现]  搜索/采集候选客户（工具调用），去重、清洗
    ▼
[3 公司背调]  抓官网/公开页面，提取业务类型、产品线、证据
    ▼
[4 客户评分]  按产品匹配度/渠道价值/市场价值/决策层线索打分
    ▼
[5 开发信草稿] RAG 检索历史开发信/产品资料/客户证据 → 个性化草稿
    ▼
[6 人工审批]  ⛔ 质量经理审批：可改可拒，批准后进入下一步
    ▼
[7 跟进计划]  按优先级/回复状态生成跟进任务与时间表
    ▼
[8 报价素材]  检索产品库/历史报价，生成报价所需素材包
    ▼
[结案]  结果入库（知识回流：本次案例供下次 RAG 检索）
```

## 3. 技术架构

| 模块 | 技术 | 职责 |
|---|---|---|
| Agent 循环 | Pydantic AI | LLM 主导 loop：每轮决策调用工具；SOP 写进系统提示词 |
| 状态/阶段机 | SDRTask（app/state.py） | 任务对象 + 9 阶段；工具带 require_stage 前置校验，流程不可乱序 |
| 工具门控 | app/tools/gates.py | 审批三元组：提交→批准→发送；send_email 无 approved 凭证即拒绝（**绝不自动发**） |
| MCP 插件 | Pydantic AI MCPToolset | Apollo.io / Clay / AdsPower 官方 MCP 即插即用，加"手脚"不改主代码 |
| LLM | Qwen2.5-7B + vLLM（本地，OpenAI 兼容端点） | 任务解析、背调总结、开发信生成、评分（模型工厂 app/llm/client.py） |
| RAG | bge-m3 + FAISS | 历史开发信/产品资料/客户证据检索，生成带引用内容 |
| 服务 | FastAPI | 任务 API、飞书回调、审批回调 |
| 飞书 | 开放平台 API | 机器人接任务、审批卡片、进度通知、多维表格落数据 |
| 前端 | Streamlit | 任务轨迹面板（执行到哪步/工具调用入参出参/卡点原因） |
| 部署 | Docker + docker-compose | vLLM 推理服务、API、UI 容器化 |
| 后端工程 | FastAPI + async + APScheduler + queue | 任务调度、后台 worker、日志/指标、任务轨迹持久化 |

## 4. 差异化（面试防御）

- **vs 第一版 export_skills**：代码重写，流程从"提示词文档"变为"可执行 Agent 循环+工具调用"。**框架选择说明**：2026 年共识是"单 agent + 少量工具，手写 loop 就够"，LangGraph 状态机过重（最陡峭的学习/维护成本），故选 Pydantic AI（MCP 支持最全、provider 中立，可接 vLLM/DeepSeek/Qwen 本地）。
- **vs Dify 低代码管道（海岸线 8D Agent 等）**：生命周期管理（状态持久化/回退/挂起）、
  闭环跟踪（措施到期催办）、知识回流（结案自动入库）、审批留痕审计。
- **vs OpenClaw 通用 agent**：垂直 SDR 岗位化、工具权限收敛、审批制度、审计合规，
  回答"AI 员工凭什么能进企业"。

## 5. 数据准备（全部公开/合成，零保密风险）

| 数据 | 内容 | 来源 |
|---|---|---|
| config/PRODUCT.yaml | 产品线（示例：户外用品/医疗耗材） | 自写配置 |
| config/MARKET.yaml | 目标市场（美国/欧盟）参数 | 自写配置 |
| config/TONE.yaml | 开发信语调模板 | 自写配置 |
| config/EMPLOYEE.yaml | AI 员工定义：SOP 流程、工具白名单、审批规则 | 自写配置 |
| data/raw/ | 候选客户样例 CSV（构造数据） | 合成 |
| data/knowledge/ | 历史开发信样例、产品资料、历史案例 | 合成+公开资料 |

## 6. 4 周里程碑

| 周 | 目标 | 验收 |
|---|---|---|
| W1 | 环境就绪：vLLM 部署 Qwen2.5-7B（或在线 API 折中）、bge-m3+FAISS 检索跑通、Pydantic AI agent 空循环 + 工具注册 | curl 推理返回、检索返回 TopK、工具列表可见 |
| W2 | 核心链路：任务解析→客户发现→背调→评分→开发信草稿（RAG 引用） | 端到端跑通 5 个样例客户 |
| W3 | 工程化：审批点+幂等+审计、跟进计划、报价素材、知识回流入库 | 审批通过/拒绝两条路径可用 |
| W4 | 飞书接入+轨迹面板+Docker 部署+测试（幂等/并发/故障）+简历措辞 | 飞书派单全流程演示，测试通过 |

## 7. 合规边界（红线）

- 邮件默认只生成草稿，**绝不自动发送**；发送必须人工审批且逐封确认。
- 不编造客户事实、联系人姓名、采购意图；每条线索和个性化表达带来源。
- API key / 真实客户名单 / 价格表不提交 Git（.env + .gitignore）。
- 用合成数据演示；不接入任何真实外贸客户系统。

## 8. 参考清单

- 第一版：github.com/Xuxchloris/export_skills（业务流程图、配置设计、合规设计）
- Pydantic AI 官方文档（Agent loop / tools / MCPToolset / OpenAI 兼容端点）
- 官方 MCP：Apollo.io（mcp.apollo.io/mcp）、Clay（api.clay.com/v3/mcp）、AdsPower（npx local-api-mcp-typescript）
- 飞书开放平台：机器人、事件订阅（im.message.receive_v1）、消息卡片、多维表格 API
- 商业对标：AI SDR（海外 SaaS 品类）、海岸线科技 8D Agent（Dify 版，看短板）

# dsh-sdr 验收清单

目标版本：DSH `0.1.0-rc.6`。验证日期：2026-08-17。

| 项目 | 结果 | 证据/说明 |
|---|---|---|
| `dsh plugin --profile web add` 安装 | 通过 | 隔离 `DSH_HOME` 下用本地 `packages/dsh-sdr` 成功添加 |
| 受管 preset 写入 | 通过 | `$DSH_HOME/.agent-presets/sdr` 生成 `agent.cordis.yml`、`preset.yml` 和管理标记 |
| 模式菜单 roster 出现 SDR | 通过 | `agentPreset.list` 返回 `sdr / user / SDR 数字员工`，无 `broken` |
| DSH 实际挂载 SDR preset | 通过 | `session.create(agentPreset=sdr)` 返回 `ok: true` |
| Python 九阶段状态机 | 通过 | MCP 阶段工具复用 `app/state.py` 的阶段校验；离线演示脚本覆盖完整路径 |
| 未批准不能推进 | 通过 | 门控烟测输出 `unapproved advance rejected` |
| 草稿变更使批准失效 | 已覆盖 | `tests/test_gates.py`：草稿哈希不一致抛出 `ApprovalError` |
| 批准后推进到结案 | 已覆盖 | `tests/test_dry_run_flow.py`：批准后 follow-up、quotation、close |
| 工具调用审计可回放 | 已实现 | `get_audit_log` MCP 工具和 loopback `/control/audit` |
| 无凭证 dry-run / draft-only | 已实现 | `DSH_SDR_DRY_RUN` 默认开启，发送出口只返回 `draft-only` |
| Python pytest 全量通过 | 未在本机执行 | 当前 WSL 环境缺少 `pydantic_ai`，测试安全跳过 2 项；依赖完整环境应运行 `pytest -q` |
| 真实 Web UI 点击录屏 | 未执行 | 已完成 API/preset 挂载验收；需要用户本地模型配置和浏览器操作 |

## 复现命令

```powershell
dsh plugin --profile web add .\packages\dsh-sdr
$env:DSH_SDR_DRY_RUN = '1'
python -m app.mcp.server --transport streamable-http
dsh web
python scripts/demo_dsh_sdr.py
```

安装器不覆盖没有 `.dsh-sdr-managed.json` 标记的同名用户 preset；验证时使用隔离 `DSH_HOME`，不会读取或提交现有 profile 凭证。

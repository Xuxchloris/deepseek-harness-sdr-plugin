# dsh-sdr 验收清单

目标版本：DSH `0.1.0-rc.6`。V2 验证日期：2026-08-17。

| 项目 | 结果 | 证据/说明 |
|---|---|---|
| Node bundle 可加载 | 通过 | `node --check packages/dsh-sdr/lib/domain.js`、`lib/index.js` |
| Node 离线回归测试 | 通过 | `npm.cmd test --prefix packages/dsh-sdr`：5 tests passed |
| 九阶段顺序 | 通过 | 服务端只提供 `sdr_next_step`，测试覆盖 task_parse 到 close |
| 审批前不能推进 | 通过 | `sdr_continue_after_approval` 在无批准凭证时失败 |
| 草稿哈希绑定 | 通过 | 修改草稿后 `sdr_review_drafts` 拒绝哈希不匹配 |
| 全局客户去重 | 通过 | Lead Registry 按 canonical lead ID 跨活动排除已 claim 客户 |
| 相同活动幂等 | 通过 | 相同 request/market/product/campaign_version 返回原 task_id |
| connector 预留 | 通过 | email、whatsapp、crm 已注册；默认 `dry_run: true` |
| Agent 部署配置覆盖 | 通过 | `DSH_SDR_AGENT_CONFIG=1` 后可写白名单参数，部署基线保留，敏感值拒绝 |
| 外部发送安全 | 通过 | 无 `send_email` 工具；DryRunConnector 返回 `blocked-dry-run` |
| 原子持久化 | 通过 | JSON 临时文件写入后 rename，测试验证状态可恢复 |
| 工具调用审计 | 通过 | `sdr_audit_log` 读取带 task/stage/action/detail 的事件 |
| DSH preset 安装 | 待用户本机执行 | `dsh plugin --profile web add "E:\\ai-sdr\\packages\\dsh-sdr"` |
| 模式菜单出现 SDR | 待用户本机执行 | 重启 `dsh web`，新建会话选择「SDR 数字员工」 |
| Web UI 端到端审批 | 待用户本机执行 | 派单后选择全部草稿，再调用后续阶段 |
| 真实渠道发送 | 不在当前范围 | 默认 dry-run；需部署方显式注册 connector 和凭证 |

## 本地演示

```powershell
npm.cmd test --prefix "E:\ai-sdr\packages\dsh-sdr"
node "E:\ai-sdr\scripts\demo_dsh_sdr.mjs"
dsh plugin --profile web add "E:\ai-sdr\packages\dsh-sdr"
dsh web
```

插件重载后要新建会话；旧会话可能仍持有旧 preset。V2 不需要启动 Python MCP 或监听 `8765`。原 Python 测试和 FastAPI/飞书路径仍保留，可在完整 Python 依赖环境中单独运行。

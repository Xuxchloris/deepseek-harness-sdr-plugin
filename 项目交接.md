# ai-sdr 项目交接文档

> 交接时间：2026-08-17
> 项目目录：`E:\ai-sdr\`
> 项目定位：外贸获客 AI 员工（SDR Agent），可插拔插件化数字员工的垂直实现
> 当前状态：**核心骨架已完成，端到端可演示；真实外部系统和发送通道未接入**

---

## 1. 一句话说明

把「开发 50 个美国户外用品客户」这类模糊指令，交给 AI 员工自动执行：

> 任务解析 → 客户发现 → 公司背调 → 客户评分 → 开发信草稿 → 人工审批 → 跟进计划 → 报价素材 → 结案

全程轨迹可回放，邮件未审批绝不发送。

---

## 2. 当前进度（按模块核对）

### ✅ 已经完成并能跑

| 模块 | 文件 | 完成度 |
|---|---|---|
| 9 阶段状态机 | `app/state.py` | ✅ 完整 |
| Agent 循环 | `app/agent/agent.py` | ✅ 完整 |
| 全部 9 阶段工具 | `app/tools/*.py` | ✅ 工具齐全 |
| 工具阶段校验 | `app/state.py` 的 `require_stage` | ✅ 越权调用会抛错 |
| 审批门控 | `app/tools/gates.py` | ✅ 完整 |
| 邮件合规护栏 | `app/guardrails.py` | ✅ 完整 |
| 开发信生成 + 重写 | `app/tools/email.py` | ✅ 完整 |
| MCP 插件加载器 | `app/tools/mcp.py` | ✅ 代码完成，未联调真实 MCP |
| RAG 检索器 | `app/rag/retriever.py` | ✅ 代码完成 |
| LLM 模型工厂 | `app/llm/client.py` | ✅ 完整 |
| FastAPI 任务/审批 API | `app/api/main.py` | ✅ 完整 |
| 飞书长连接机器人 | `app/feishu/bot.py` | ✅ 代码完整 |
| 一键 Demo | `scripts/run_demo.py` | ✅ 完整 |
| 合成样例数据 | `scripts/sample_data.py` | ✅ 完整 |
| Docker 文件 | `Dockerfile`、`docker-compose.yml` | ✅ 已提供 |

### ⚠️ 半成品 / 未验证

| 事项 | 现状 | 交接后要做什么 |
|---|---|---|
| Apollo / Clay MCP | 加载器写好，**未真实联调** | 拿真实 OAuth 跑一次，看工具 schema |
| 客户发现 | 现在只从 `data/raw/prospects.csv` 匹配 | 接 Apollo MCP 或真实搜索 |
| 公司背调 | 现在用样例数据，未抓真实官网 | 接 `web_fetch` / Scrapling |
| 邮件发送 | `send_email` 返回 `queued` + `draft-only`，**不真实发信** | 接邮件通道，保留审批前置 |
| 飞书机器人 | 代码完整，**未用真实飞书 App 验证** | 建飞书应用，跑长连接 |
| 数据持久化 | API 和飞书都用内存字典 | 生产换 PostgreSQL/Redis |
| 自动化测试 | **没有 tests 目录** | 先补状态机和审批门控的测试 |

### ❌ 明确未做

- 真实客户系统接入
- 真实邮件发送
- 登录/权限体系
- 并发任务队列
- 线上部署

---

## 3. 目录结构与关键文件

```text
E:\ai-sdr\
├── app\
│   ├── agent\agent.py       # Agent 循环、SOP Prompt、审计流、结案报告
│   ├── api\main.py          # FastAPI：/tasks /tasks/{id}/approve /tasks/{id}/run
│   ├── feishu\bot.py        # 飞书长连接机器人 + 审批卡片
│   ├── llm\client.py        # OpenAI 兼容端点模型工厂
│   ├── rag\retriever.py     # bge-m3 + FAISS 检索
│   ├── tools\               # 9 阶段工具 + gates + mcp 加载器
│   ├── guardrails.py        # 开发信合规校验
│   ├── schemas.py           # Intent / DraftEmail / SDRReport 等模型
│   └── state.py             # SDRTask 阶段机 + 落盘/恢复
├── config\
│   ├── EMPLOYEE.yaml        # AI 员工定义：SOP / 工具白名单 / 审批规则
│   ├── MARKET.yaml          # 目标市场
│   ├── PRODUCT.yaml         # 产品线
│   └── TONE.yaml            # 开发信语调
├── data\
│   ├── raw\prospects.csv    # 合成候选客户
│   ├── knowledge\           # RAG 样例知识库
│   └── exports\last_task.json  # 任务状态落盘
├── scripts\
│   ├── run_demo.py          # 一键端到端 Demo
│   └── sample_data.py       # 生成合成样例数据
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
├── README.md
└── 项目计划书.md
```

---

## 4. 怎么跑

### 4.1 环境变量（.env）

需要配置：

```text
LLM_BASE_URL=http://localhost:8000/v1
LLM_API_KEY=sk-local
LLM_MODEL=Qwen2.5-7B-Instruct

FEISHU_APP_ID=
FEISHU_APP_SECRET=

MCP_SERVERS=[]  # 可选，JSON 格式
```

### 4.2 一键 Demo

```bash
cd E:\ai-sdr
pip install -r requirements.txt
python scripts/sample_data.py
python scripts/run_demo.py "开发 5 个德国户外用品客户"
```

Demo 流程：

1. 阶段 1 自动执行到 `human_approval` 卡点
2. 脚本模拟人工批准
3. 阶段 2 续跑至结案
4. 输出结构化结案报告 + 审计日志 + 状态落盘

### 4.3 FastAPI

```bash
uvicorn app.api.main:app --host 0.0.0.0 --port 8000
```

| 接口 | 方法 | 作用 |
|---|---|---|
| `/health` | GET | 健康检查 |
| `/tasks` | POST | 创建任务 |
| `/tasks/{id}` | GET | 查任务状态 |
| `/tasks/{id}/approve` | POST | 人工审批邮件 |
| `/tasks/{id}/run` | POST | 跑 Agent 循环（首次到审批点，批准后再次调用续跑） |

### 4.4 飞书机器人

配置 `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 后启动机器人，支持：

- 自然语言派任务
- 审批卡片（全部批准按钮）
- 回复「批准」或「批准全部」
- 意图判断：闲聊不启动工作流

---

## 5. 核心设计决策（交接人必须理解）

### 5.1 为什么不用 LangGraph

单 Agent + 少量工具，手写 loop 就够；LangGraph 状态机过重。选择 **Pydantic AI 的 LLM 主导 loop + 显式阶段机**：

- Agent 在循环里自由决策
- 每个工具带 `require_stage` 阶段前置校验
- 审批点必须挂起等人工

### 5.2 为什么审批是硬门控

`approve_email` **不在 Agent 工具列表里**，模型不能自我批准。只有人工通过 API / 飞书卡片调用 `gates.approve_email` 才能批准。

`send_email` 双重保险：

1. 没 approved 凭证直接抛 `ApprovalError`
2. 即使批准，当前也只是 `queued` + `draft-only`，不真实发信

### 5.3 MCP 插件化的设计

「给员工加手脚 = 加一个 MCP server，不改主代码」。

`app/tools/mcp.py` 从环境变量 `MCP_SERVERS` 或 `config/mcp.json` 读取：

```json
[
  {"name": "apollo", "url": "https://mcp.apollo.io/mcp", "auth": "oauth"},
  {"name": "adspower", "command": "npx", "args": ["local-api-mcp-typescript"]}
]
```

### 5.4 合规护栏

开发信生成后经过 `app/guardrails.py`：

- 疑似编造联系人信息 → 让模型重写
- 疑似编造采购意向 → 让模型重写
- 重写后仍违规 → 丢弃不入库

---

## 6. 红线（不能破坏）

1. **邮件未批准绝不发送**
2. **不编造客户事实、联系人姓名、采购意图**
3. **API key、真实客户名单、价格表不提交 Git**
4. **只用合成数据演示，不接真实外贸客户系统**

---

## 7. 已知问题 / 技术债

| 问题 | 影响 | 建议 |
|---|---|---|
| `app/feishu/bot.py` 的 `send_card` 函数重复定义两次 | 低，后者覆盖前者 | 清理 |
| `discovery.py` 里 `or not rows` 逻辑绕 | 低，边界条件容易误判 | 重写匹配逻辑 |
| 任务表是内存字典 | 重启丢任务 | 换 PostgreSQL/Redis |
| bge-m3 首次调用下载模型 | 首次很慢 | 本地缓存模型目录 |
| README 里飞书还标着「占位」 | 文档过时 | 更新为已实现 |
| 没有自动化测试 | 改动容易破坏审批红线 | 优先补 gates/state 测试 |

---

## 8. 交接后建议的下一步（按优先级）

1. **补测试**：状态机越权、审批门控、guardrails 三类测试
2. **真实验证飞书闭环**：建测试飞书应用，跑「派单 → 审批卡片 → 批准 → 结案」
3. **联调一个真实 MCP**：Apollo 或 Clay，确认工具 schema 与 Agent 调用
4. **把任务表落 PostgreSQL**，飞书和 API 共用一套任务存储
5. **更新 README**，去掉「占位」描述，补充当前完成度和运行方式

---

## 9. 交接清单（待确认）

- [ ] LLM 服务地址 / API key 归属
- [ ] 飞书测试应用是否已创建
- [ ] MCP 账号（Apollo/Clay）是否已申请
- [ ] 是否已有目标云服务器
- [ ] 老项目 `export_skills` 与本项目的关系是否已记录

---

## 10. 相关文件

- 项目计划书：`E:\ai-sdr\项目计划书.md`
- 第一版 Skills：https://github.com/Xuxchloris/export_skills
- 项目源码：`E:\ai-sdr\`
